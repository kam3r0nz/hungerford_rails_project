<%= link_to "Schedule Appointment", new_appointment_path %>

<% @appointments.each do |apt| %>
    <p><%= link_to "#{apt.date.strftime("%m/%d/%Y")} - #{apt.service.name} for #{apt.animal.name}", appointment_path(apt) %></p>

<% end %>