<%= form_for @appointment do |f| %>
    <%= f.label :date %>
    <%= f.date_field :date %><br>

    <%= f.label :animal %>
    <%= f.collection_select :name, Animal.all, :id, :name %><br>

    <%= f.label :veterinarian %>
    <%= f.collection_select :name, Veterinarian.all, :id, :name %><br>

    <%= f.submit %>
<% end %>