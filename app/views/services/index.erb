<h2>Services</h2>

<% if @services.empty? %>
    There are currently no services in the system.
<% else %>
    <% @services.each do |service| %>
        <p><%= service.name %> - <small><%= link_to "Delete", service, method: :delete, data: { confirm: "Are you sure you want to delete this service?" } %></small></p>
    <% end %>
<% end %>

<hr class="small"/><br>

<%= link_to "Add Service", new_service_path, class: "btn" %>